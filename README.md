## Raymarching Engine

this is a separate repository to store my raymarching engine, because keeping it in my website repository was starting to get annoying in terms of git structure. This is the REAL repository, every other engine project I do may have copies, but this should be thought of as the master repository.

## current goals

- multiple light sources
- figure out why the page is sometimes excessively slow to load